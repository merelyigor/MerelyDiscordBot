node_modules/
dist/
.env
coverage/
*.log

# JetBrains: track shared settings, ignore user-specific
.idea/workspace.xml
.idea/shelf/
.idea/httpRequests/
.idea/datasources.xml
.idea/datasources/

# graphify: у Git лишаються ПОРТАТИВНІ артефакти графа — `graph.json` і
# `GRAPH_REPORT.md`, щоб нова сесія не починала з побудови. Решта є похідним або
# машинозалежним станом: `cache/` (SHA256-кеш), `studio/` (прегенерована SPA,
# відновлюється `graphify studio export`), `branch.json`/`worktree.json`/
# `needs_update` (стан конкретного робочого дерева, який ламає портативність).
.graphify/*
!.graphify/GRAPH_REPORT.md

# Data source для IDE: структура підключення (хост, порт, база, користувач) є
# спільною для всіх, хто працює з проєктом, тому вона у Git. Credentials JetBrains
# тримає окремо в .idea/dataSources.local.xml · цей файл лишається ігнорованим.
!/.idea/
/.idea/*
!/.idea/dataSources.xml
